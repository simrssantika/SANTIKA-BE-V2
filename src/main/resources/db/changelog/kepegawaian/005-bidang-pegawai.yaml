databaseChangeLog:
  - changeSet:
      id: bidang-pegawai
      author: ridlo
      changes:
        - createTable:
            schemaName: kepegawaian
            tableName: bidang_pegawai
            columns:
              - column:
                  name: id
                  type: uuid
                  defaultValueComputed: gen_random_uuid()
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: kode_bidang
                  type: varchar(50)
                  constraints:
                    nullable: false
              - column:
                  name: nama_bidang
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: deleted_at
                  type: timestamp
                  constraints:
                    nullable: true
              - column:
                  name: created_at
                  type: timestamp
                  constraints:
                    nullable: true
              - column:
                  name: updated_at
                  type: timestamp
                  constraints:
                    nullable: true

  - changeSet:
      id: bidang-pegawai-index
      author: ridlo
      changes:
        - sql:
            dbms: postgresql
            splitStatements: true
            sql: |
              CREATE UNIQUE INDEX idx_unique_kode_bidang_pegawai ON kepegawaian.bidang_pegawai (kode_bidang) WHERE (deleted_at IS NULL);
